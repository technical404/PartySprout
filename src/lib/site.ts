/** Shared site-level constants. */

export const SITE_NAME = "PartySprout";

/**
 * Displayed contact address. Uses the reserved `.example` TLD (RFC 2606) so the
 * placeholder can never reach a real inbox — replace it with the real support
 * address before launch.
 */
export const SUPPORT_EMAIL = "hello@partysprout.example";

/** Last review date shown on the Terms and Privacy pages. */
export const LEGAL_LAST_UPDATED = "September 2026";
