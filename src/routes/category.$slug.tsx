import { createFileRoute } from "@tanstack/react-router"; import { SearchResults } from "@/components/marketplace/marketplace";
export const Route=createFileRoute("/category/$slug")({head:({params})=>{const name=params.slug.replaceAll("-"," ");return{meta:[{title:`${name} for Kids' Parties — PartySprout`},{name:"description",content:`Compare trusted ${name} entertainers, prices, reviews and availability near you.`},{property:"og:title",content:`Find ${name} for children's parties`},{property:"og:description",content:`Compare trusted local ${name} and request free quotes.`},{property:"og:type",content:"website"},{property:"og:url",content:`/category/${params.slug}`},{name:"twitter:card",content:"summary_large_image"}],links:[{rel:"canonical",href:`/category/${params.slug}`}]}} ,component:CategoryPage}); function CategoryPage(){const {slug}=Route.useParams();return <SearchResults title={`${slug.replaceAll("-"," ")}`} category={slug}/>}